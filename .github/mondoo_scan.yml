name: mondoo

on:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Scan with Mondoo
        uses: tas50/mondoo-github-action@main
        with:
          path: 'nginx.yaml'